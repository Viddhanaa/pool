╔══════════════════════════════════════════════════════════════╗
║         HƯỚNG DẪN NHANH - TRUY CẬP TỪ MÁY HOST               ║
╚══════════════════════════════════════════════════════════════╝

⚠️  BẠN ĐANG Ở TRONG VM - CẦN SETUP TỪ MÁY HOST!

📍 Bước 1: TÌM TÊN VM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Trên MÁY HOST (không phải trong VM), mở Terminal/CMD:

   VBoxManage list vms

Ví dụ output: "Ubuntu" {xxx-xxx-xxx}
→ Tên VM của bạn là: Ubuntu


📍 Bước 2: CHẠY LỆNH PORT FORWARDING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Thay "Ubuntu" bằng tên VM của bạn, chạy trên MÁY HOST:

# Web Dashboard
VBoxManage modifyvm "Ubuntu" --natpf1 "asd-web,tcp,127.0.0.1,4173,10.0.2.2,4173"

# Admin Dashboard  
VBoxManage modifyvm "Ubuntu" --natpf1 "asd-admin,tcp,127.0.0.1,4174,10.0.2.2,4174"

# Backend API
VBoxManage modifyvm "Ubuntu" --natpf1 "asd-backend,tcp,127.0.0.1,4000,10.0.2.2,4000"

# BlockScout Explorer
VBoxManage modifyvm "Ubuntu" --natpf1 "blockscout-web,tcp,127.0.0.1,4001,10.0.2.2,4001"

# BlockScout API
VBoxManage modifyvm "Ubuntu" --natpf1 "blockscout-api,tcp,127.0.0.1,4002,10.0.2.2,4002"

# Geth RPC
VBoxManage modifyvm "Ubuntu" --natpf1 "geth-rpc,tcp,127.0.0.1,8545,10.0.2.2,8545"

# Grafana
VBoxManage modifyvm "Ubuntu" --natpf1 "grafana,tcp,127.0.0.1,3000,10.0.2.2,3000"


📍 Bước 3: TRUY CẬP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Mở browser trên MÁY HOST:

🌐 Web Dashboard:    http://localhost:4173
👨‍💼 Admin Dashboard:  http://localhost:4174
🔍 BlockScout:       http://localhost:4001
📊 Grafana:          http://localhost:3000
⚙️  Backend API:      http://localhost:4000/health


📍 CÁCH KHÁC: DÙNG GUI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Mở VirtualBox Manager
2. Chọn VM → Settings → Network → Adapter 1 → Advanced
3. Click "Port Forwarding"
4. Thêm các rule theo bảng trong file VM_PORT_FORWARDING.md


💡 LƯU Ý:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Chạy lệnh trên MÁY HOST (Windows/Mac/Linux), KHÔNG phải trong VM
- Có thể cần restart VM sau khi thêm rules
- Nếu port bị chiếm, đổi host port (vd: 14173 thay vì 4173)


📚 CHI TIẾT:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Xem file: VM_PORT_FORWARDING.md

